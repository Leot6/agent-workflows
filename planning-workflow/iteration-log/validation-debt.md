# planning-workflow — validation-debt ledger (A§11)

> Open rows in full; closed rows prune to the INDEX. `M` = model-dependency:
> justification cites a measured model behaviour; the register outlives closure.

| debt id | mechanism | seed basis | M | closes when | firing evidence | topics elapsed |
|---------|-----------|-----------|---|-------------|-----------------|-----|
| VD-1 | grill | owner-directed seed (A§13 seeds — extraction and review instruments) | — | a live grill surfaces a decision that would otherwise arrive as a late owner correction | | 2 |
| VD-4 | cold/warm table **with the round-temperature dimension** (construction-form decidability at protocol §4.6.4, warm preconditions ①–④ with provenance-carry, anti-gaming revert, cold delta lens as check ③) | seed + owner-queue ruling | **M** | a cold re-read catches a warm-session miss on a live topic; for the temperature dimension: a warm-eligible round whose close-out later shows a zero-context gap (cold author needs more than artifacts) ⇒ the table reverts to all-cold (the ruling's rule-level fallback) | partially fired: warm revisions on a live topic were caught by cold delta reviews (rule and wording findings, some after publication); n=1 | 2 |
| VD-8 | specification ledger (A§5.3) | build-round observation | — | a full-tree review uses it to discharge or convict a delegation | partially fired: a full-tree round convicted a delegation boundary, and a release round discharged the ledger's rows and convicted a review-unit orphan | 2 |
| VD-9 | shared-understanding section (as a workflow stage) | pre-workflow campaign precedent | — | a live topic's owner veto lands on a shared-understanding claim | partially fired: a design-section approval was reversed by an offline owner ruling; a veto on an SU claim proper is still awaited | 2 |
| VD-10 | iteration-log state file (prune-at-retro + one-line index) | owner-directed seed (A§13 seeds — self-iteration and metrics) | — | a retro prunes closed rows and a later anchor match uses the index | the prune half fired at two retros; the later-anchor-match half remains | 2 |
| VD-12 | third coverage direction (item → scope/row) | owner-directed seed (A§13 seeds — self-iteration and metrics) | — | the gate row convicts an unmapped item on a live topic | not fired by the gate row: one topic exited via scope-out before a convergence gate; on another, signed rows outside the coverage table were convicted at disposition, not by the gate | 2 |
| VD-13 | escape count / planning-DRE | owner-directed seed (A§13 seeds — self-iteration and metrics) | — | a full-amend row's cause dating feeds a cross-topic comparison | one real datum (one topic: 1 full-amend arc whose causes predate convergence) and one 0-by-construction baseline (§4.11.6: a baseline, not a result) | 2 |
| VD-18 | intent verification cell = shared understanding (SU table + every brief item's worked example) | owner-directed seed (A§13 seeds — validation and the boundary's return path) | — | a live topic's owner veto lands on an SU claim about **intent** | partially fired (the same design-section reversal as VD-9 — an approval's premise, not an SU-claim veto proper) | 2 |
| VD-19 | the gate's fourth coverage direction (approved scope → recorded topic statement) + the design doc's traces-to column | owner-directed seed (A§13 seeds — validation and the boundary's return path) | — | P-2b or the meta-critic's statement comparison convicts a bullet with no fragment behind it, or an ask in the statement that no bullet reaches | P-2b executed clean on a live topic (fragment-by-fragment comparison, zero convictions — exercise without conviction) | 2 |
| VD-20 | post-convergence amend entry line + classify-first ordering | owner-directed seed (A§13 seeds — validation and the boundary's return path) | — | an amend session enters through the line and its cause paragraph dates a full-amend row — the first escape count that is not 0 by construction | not fired, measured the negative way: a topic's first amend arc entered through an owner ruling outside the protocol line, leaving no amend row | 2 |
| VD-22 | prescription shape (`claims.md`) + the item scaffold's `prescriptions` field | two anchors from one live delivery topic, different stages and lineages: a plan imperative ordering commit-message content the target repo's hook refuses, and an instruction placing an edit inside a commit whose governing whitelist closes with "and nothing else". Both from a plan this workflow did not produce — the recurrence is across stages, not topics. Owner-directed landing | — | a live topic fills the `prescriptions` field and a named rule turns out to refuse what an imperative prescribed — or an executor meets a prescription the field answered "none" to | | 1 |
| VD-24 | maintainer entry-line item slot (protocol §7: `between-rounds edits` may carry `; item(s): <what + ABS pointer to the signed authority>`) | **owner-directed seed** (A§1 carve-out ii; recorded A§13): a signed ruling dispatched as `task between-rounds edits` was lost — dispatch, receipt and execution were each correct, but that closed-set value carried no item position, and a maintainer onboards from the line alone (A§3) | — | a maintainer session dispatched from §7's line **with** the slot lands the item it names, on a dispatch that read the form out of §7 rather than hand-rolling it — **or** the refutation: a slotted dispatch still loses the item, which says the slot was not the binding constraint | partially fired before landing, both directions on one ruling: the unslotted dispatch lost it for a round, a hand-rolled slotted dispatch landed it. Not yet evidenced: the slot read from §7 by a dispatcher who did not already know the workaround | 1 |

The ladder ruling (A§13 instrument ladder ordered by independence distance) is
signed with its landing sequenced and carries **no debt row yet**: debt is
exercise evidence owed by a mechanism that is in the tree, and this one is not.

Carried mechanisms (closure-proof, fix claims, carry-forward, restart) enter
with prior live-run evidence cited in `design/rationale.md` — no debt row.
