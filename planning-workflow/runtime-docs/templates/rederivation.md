# Re-derivation record  (template; cap `DOC_CAP_TEMPLATE`)
<!-- round-0 entries append to planning/rederivations.md; in-round entries to
     rounds/<n>/rederivation_<k>.md. One entry per owner reply (A§2). -->

## R-<k> — <one-line subject>
- **Reply (carried text — quoted verbatim, source: brief/grill ref)**: …
- **Premise re-derivation** (FIRST — does the reality the reply asserts hold
  against code?): this record is what discharges the **decision-claim** shape
  (`claims.md`), whose precondition is the premise before the landing site —
  an owner reply is provenance-required, not self-certifying. Claim → evidence
  (`file:line` opened this session; shape-keyed per `claims.md`). Special cases: the reply **reverses observed code** →
  re-derive the reversal point itself; the reply **rests on an external
  behavior** → name the written contract obligation and enumerate the whole
  family of same-shape dependencies now (family shape).
- **Landing site** (second — where the decision realizes in code): `file:line`.
- **Verdict**: holds / holds-with-correction: <what the owner's wording missed,
  raised back as a brief item> / contradicts-code: <ruling-contradiction brief
  ref>.
- **Baked into**: <artifact + section, or "not yet">.

Recording ≠ disposition: a re-derived reply still closes only through its
signed decision-log row.
